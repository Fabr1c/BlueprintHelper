#!/usr/bin/env node

console.error('[BlueprintHelper] Global MCP is for editor lifecycle plus developer-only exec. Ordinary BlueprintHelper reads and writes must use the CLI; mcp__blueprint_helper__blueprint_developer_exec_console_command is not an ordinary Agent workflow tool.');
