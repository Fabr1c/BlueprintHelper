# MCP Regression Fixtures

These payloads exercise public MCP compatibility around legacy export scopes,
strict import defaults, and safety result fields.

The target asset paths are placeholders for editor-backed acceptance checks.
The MCP server should add `auth_token` from `BLUEPRINTHELPER_BRIDGE_TOKEN`;
callers should not provide it manually.
