# Final Report Policy

普通完成报告包含：

```text
任务状态
目标资产
主要变更摘要
验证结果：compile / diagnostics / save
未完成项或用户需要动作
```

默认不展开：完整 TaskPlan、child transaction_id 列表、Journal 路径、Review Store 路径、完整 diff、schema 细节、底层 Bridge 原始 JSON。
